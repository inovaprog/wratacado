<!DOCTYPE html>
<html>

<head>
  <title>Lista de Produtos</title>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
    integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">


</head>

<body>

  <div class="container">
    <h1>
      <center>Lista de Produtos</center>
    </h1>

    <div style="padding: 20px;">
      <form action="/add" method="POST">
        <button type="submit" class="btn btn-primary">Adicionar Novo</button>
      </form>
    </div>


    <table border=1 cellspacing=0 style="width:100%">
      <thead>
        <tr style="background-color: #CCC">
          <td style="width:5%">Imagem</td>
          <td style="width:15%">Produto</td>
          <td style="width:40%">Descrição</td>
          <td style="width:15%">Categoria</td>
          <td style="width:5%">Valor</td>
          <td style="width:5%">Editar</td>
          <td style="width:5%">Excluir</td>

        </tr>
      </thead>
      <tbody>
        <% if(!produtos || produtos.length == 0) { %>
        <tr>
          <td colspan="2">Nenhum item cadastrado.</td>
        </tr>
        <% } else {produtos.forEach(function(item){ %>
        <tr>

          <td style="width:15%">
            <center><img style="width: 100px; height: 60px;"
                src="https://uploadwr.s3-us-west-1.amazonaws.com/<%=item.foto%>">
              <form method="POST" action="https://wratacado.appspot.com/api/foto/<%= item._id %>" enctype="multipart/form-data">
                <!-- -->
                <div class="form-group">
                  <input type="file" class="form-control-file" id="exampleFormControlFile1" name="file">
                </div>
                <button type="submit" class="btn btn-primary">Trocar Foto</button>
              </form>
            </center>
          </td>


          <td style="text-transform: uppercase;"> <%= item.titulo %></a></td>
          <td style="width:40%"><%= item.descricao %></td>
          <td style="width:15%"><%= item.categoria %></td>
          <td style="width:10%"><%= item.preco %></td>
          <td><a href="https://wratacado.appspot.com/edit/<%= item._id %>">Editar</a></td>
          <!-- -->
          <td><a href="https://wratacado.appspot.com/api/delete/<%= item._id %>"
              onclick="return confirm('Tem certeza?');">Excluir</a></td>
          <!-- -->
        </tr>
        <% })
   }%>
      </tbody>
    </table>

  </div>


</body>

</html>